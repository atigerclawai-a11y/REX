# TECH_DEBT.md — GOJ OCR Pipeline Technical Debt Register

> **Generated:** 2026-08-05 05:52 EDT  
> **Severity scale:** CRITICAL (stops meal service) > HIGH (client missing sheets) > MEDIUM (operational friction) > LOW (cosmetic/edge case)  

---

| # | Severity | Description | Impact if it fails | What depends on it | Rough repair effort | Business risk |
|---|---|---|---|---|---|---|
| 1 | **CRITICAL** | `day_*_actual` clobbered by 4+ daily cron jobs (6am, noon, 17:10, 20:00) that re-sync from stale Drive tabs | Attendance counts silently change mid-day; sheets regenerate with wrong client lists; staff serves wrong clients | Sign-in, kitchen, distribution, driver sheets — ALL generators | 4h (skill prompt hardening + per-cron guard) | Client served wrong food or missed entirely; compliance/audit failure |
| 2 | **CRITICAL** | `CC_unified_sheets.py` reads from `ghs_schedule.db` (RETIRED per Kato Aug 3) instead of `auth_tracker.db` | Sign-in and driver sheets have wrong attendance counts (Wed 64 vs real 73, Thu 118 vs real 88) | Sign-in sheets, driver routes | 2h (repoint DB + fix 545-line script) | Staff uses wrong sign-in roster; drivers pick up wrong clients |
| 3 | **CRITICAL** | `generate_tomorrow.py` requires Python 3.10+ (PEP 604 `|` syntax) but system `python3` is 3.9.6 | Generator fails with TypeError on line 415; sheets cannot be generated without finding Python 3.10+ with reportlab | All daily sheet generation | 1h (2-line fix: `Optional[X]` instead of `X \| None`) | Staff cannot print today's sheets |
| 4 | **HIGH** | `goj_kitchen_paired.py` JOIN silently drops clients missing from proprietary `clients` table | 1-2 clients per shift vanish from kitchen sheets with no error message | Kitchen count sheets | 1h (LEFT JOIN + warn on missing) | Client's meal not prepared |
| 5 | **HIGH** | Partial OCR rows (exists but has empty cells) defeat the fill chain | Client with a partial row is treated as "already filled" — vanishes from kitchen/distribution while remaining on sign-in | CC_menu_fill.py, kitchen, distribution | 3h (pre-fill sweep + bulk top-up script) | 5+ clients/week silently dropped |
| 6 | **HIGH** | Blue-ink checkmarks invisible to focr (reads names fine, returns empty `marks: {}`) | Multi-hour focr batch silently produces empty plates for blue-ink forms | OCR extraction → apply chain | 2d (vision-based fallback needed) | Forms read as blanks; clients get house_standard instead of their picks |
| 7 | **HIGH** | Tilted/unpaired pages silently dropped by page-pairing reader | 24 pages across 7 batches never read; hidden forms with real client orders | OCR intake → apply chain | 4h (page_census.py + unpaired recovery) | Client's week of picks never makes it to kitchen |
| 8 | **HIGH** | Surya/focr produce abbreviations (Вин, Кур, MP, FF, Гр) that fail the dish catalog contract gate | 1,400+ rows quarantined in a single batch; valid picks rejected | write_blank_picks.py, pre_generation_gate.py | 4h (canonicalize before contract check) | Real client orders treated as invalid |
| 9 | **HIGH** | History carry-forward re-infects current week with abbreviations if history wasn't canonicalized | Fill chain copies abbreviated dishes from old rows → kitchen sheets show garbage names (Вин, MP under SALADS) | CC_menu_fill.py, all generators | 2h (canonicalize_all_history.py must run after every alias update) | Kitchen sheets show wrong/duplicate dish names |
| 10 | **MEDIUM** | `generate_tomorrow.py` day-name resolution is date-relative; after midnight, `--day Tuesday` resolves to NEXT week | "0 menu orders" false alarm; wrong sheets generated for wrong date | Sign-in, drivers, distribution generation | 1h (add `--date` absolute mode to CLI) | Staff gets next week's sheets instead of today's |
| 11 | **MEDIUM** | Proprietary DB sync is one-way (REX→Documents); Documents-only writes silently reverted within 5 minutes | OCR conversions, manual fills, and shift fixes vanish; appears to "work" then disappears | All manual/semi-automated writes | 2h (dual-write every path or make sync bidirectional) | Data loss during critical fix sessions |
| 12 | **MEDIUM** | Cross-shift duplicates break shift-fix UPDATEs (`UNIQUE(client_name, menu_date, shift)` constraint) | IntegrityError when fixing a client's wrong-shift row; requires delete-then-insert dance | Shift correction workflow | 1h (detect-before-update pattern) | Time wasted during urgent sheet fixes |
| 13 | **MEDIUM** | `generate_tomorrow.py draw_kitchen()` caps salads/soups at 6 rows and combos at 10, while SECTION TOTAL counts everything | Kitchen count sheet shows totals that don't add up; 31 of 72 hot portions invisible one day | Kitchen sheets (if using generate_tomorrow for kitchen) | 1h (remove caps + paginate) | Kitchen under-prepares; clients don't eat |
| 14 | **MEDIUM** | `CC_drive_preflight.py` embedded preflight() zeroes ALL `day_*_actual` when Drive is unreachable | 32 Tue + 18 Wed clients zeroed in one run; sheets regenerated with wrong counts | Attendance truth, all sheet generators | 2h (guard: never write actuals from Drive, only from Carecenta live) | Staff uses wrong attendance roster |
| 15 | **MEDIUM** | Surname-fragment matching over-matches (Safonov→Safonova) | Client gets relative's meal order carried as their "last order" | last_order_fallback fill chain | 2h (require ratio ≥0.80 on full name, not fragment) | Wrong food served to client |
| 16 | **MEDIUM** | Carecenta↔auth name reconciliation uses reversed name orders (Last First vs First Last) | Every client appears as "NOT IN AUTH" on first pass | Attendance sync, sheet generation | 1h (standardize normalization function) | Wrong attendance counts; over/under reporting |
| 17 | **MEDIUM** | `cc_local_pa_integrity.py` exits non-zero for non-actionable findings (Pattern #5) | Recurring false-positive cron error; trains operators to ignore real errors | cron health monitoring | 30m (exit 0 for warnings, exit 1 for errors only) | Real errors missed in noise |
| 18 | **MEDIUM** | Cloud timeout cluster (600s non-streaming API idle) hits 15+ cron jobs simultaneously during morning load | 48% of crons fail during 08:00-11:00 window; audit blind spots | Red Team, Blue Team, Memory Injector, NotebookLM | 4h (provider-side or streaming mode) | 4h audit gap daily |
| 19 | **LOW** | focr batch runs for days on single doc (doc006880, 62 pages, 4+ days, 730% CPU) | swap pressure, load average spikes, other jobs timeout | System resources, cron reliability | 2h (concurrency cap + stuck-page detection) | Resource cascade, degraded system |
| 20 | **LOW** | `generate_distribution_sheet.py` sqlite3.Row `.get()` crash (fixed Aug 5 but patch is in untracked file) | Distribution sheet generation fails silently | Distribution sheets | 30m (bracket access `menu["source_sheet"]` not `.get()`) | Distribution sheets missing |
| 21 | **LOW** | ghs_schedule.db still exists on disk (557KB) but is RETIRED | `CC_unified_sheets.py` still reads from it; confusion about which DB is canonical | Sign-in/driver generation (if using wrong generator) | 30m (rename to `.retired` or delete) | Accidental use of stale data |
| 22 | **LOW** | `/Volumes/cartoons` has Spotlight indexing enabled on a volume with client data | Spotlight index carries client names onto internal SSD outside the encrypted volume | PHI boundary | 15m (`mdutil -i off /Volumes/cartoons`) | PHI leakage through Spotlight |
| 23 | **LOW** | `GOJ_Menu_Orders.json` has 3 copies; only 1 is the runtime path | Stale JSON → "0 menu orders" false alarm on sheet generation | generate_tomorrow.py | 30m (single canonical path + symlinks) | Confusion during urgent regeneration |
| 24 | **LOW** | `week30_dishes.json` catalog locked by sha256 but no automated hash verification on access | Catalog could be silently modified; no alert | All dish validation | 1h (hash check in pre_generation_gate) | Unknown dish names in sheets |
