HERMES TELEGRAM BOT OUTAGE — INCIDENT REPORT
Gold Health Systems · Generated by Claude · 2026-06-04
═══════════════════════════════════════════════════════════════

ROOT CAUSE
───────────
HTTP 409 Conflict — multiple instances of the same bot scripts are
all polling the same Telegram token simultaneously. When Telegram sees
two getUpdates requests for the same token, it returns 409 to both,
effectively killing all of them. This explains why ALL bots are down.

EVIDENCE
────────
1. logs/rex_bot_stderr.log (9.2 MB, active):
   - First line: "🦖 REX Telegram bot started (chairman_chat_id=5587703834)"
   - Lines 1–~115k: HTTP Error 401: Unauthorized  (bad/old token)
   - Lines 115k+:   HTTP Error 409: Conflict       (duplicate instance)

2. logs/rex_telegram_stderr.log (388 KB, active):
   - All lines: HTTP Error 409: Conflict

3. logs/rexxie_bot_stderr.log (22 MB, active):
   - First block: python: not found / getcwd permission errors
     → Zombie plist (com.hermes.rexxie-bot.plist) crash-looping
   - Later block: HTTP Error 409: Conflict
     → Legit rexxie instance also blocked by conflict

4. logs/CC_fix_all.log (5:43 AM run):
   - Hermes local gateway (65001) failed with:
     "Telegram bot token already in use (PID 13487). Stop the other
      gateway first."
   - Cloud gateway ran but Telegram platform failed to connect

5. Quarantine log (2:00 PM today):
   - com.hermes.cloud-workspace.plist ✅ ALREADY UNLOADED
   - com.hermes.workspace.plist       ✅ ALREADY UNLOADED
   These were causing interference earlier — already remediated.

BOT TOKEN MAP (confirmed from config files)
────────────────────────────────────────────
  rex_telegram_bot.py       → rex_telegram_config.json       → token: 8649027922...
  rex_rexxie_telegram_bot.py → rex_rexxie_telegram_config.json → token: 8657319466...
  Hermes cloud gateway      → ~/.hermes/profiles/cloud/config.yaml → separate token

BOTS STATUS PRE-FIX
────────────────────
  @Hermes_Cloud_May_bot    ❌ DOWN  (gateway process killed by PID conflict earlier)
  @goldhealth_rexxie_bot   ❌ DOWN  (409 — zombie plist + legit instance competing)
  @HermieChatt_bot         ❌ DOWN  (local gateway 65001 failed to start)
  @RexOfGold_bot           ❌ DOWN  (409 — multiple rex_telegram_bot.py instances)
  @GOJReceipts_bot         ❌ UNKNOWN (depends on which service drives it)
  @GojAttendance_bot       ❌ UNKNOWN (depends on which service drives it)

FIX DEPLOYED
────────────
Script: ~/Desktop/REX/CC_bot_fix_TIMESTAMP.command

The fix:
  1. pkill -9 all rex_telegram_bot.py instances
  2. pkill -9 all rex_rexxie_telegram_bot.py instances
  3. pkill -9 all Hermes gateway processes
  4. Check + suppress zombie plist (com.hermes.rexxie-bot.plist)
  5. Unload ai.hermes.gateway-cloud.plist, com.rex.backend.plist cleanly
  6. Wait 8s for Telegram to release sessions
  7. Reload ai.hermes.gateway-cloud.plist → port 3002
  8. Reload com.rex.backend.plist → port 8000
  9. Live token validation via Telegram API getMe

TO RUN THE FIX
──────────────
  Double-click: ~/Desktop/REX/CC_bot_fix_TIMESTAMP.command
  OR in Terminal: ~/Desktop/REX/CC_bot_fix_TIMESTAMP.command

OPEN ITEMS (requires Kato action)
────────────────────────────────
  1. If bot still shows 401 after fix:
     Token may be revoked — regenerate via @BotFather and update config JSON

  2. CLAUDE.md flags open security item:
     Phase 16 audit: "Telegram bot token in plaintext — 33 files.
     ACTION: revoke + regenerate + Keychain only"
     → This is the reason you had a 401 on rex_telegram_bot — old plaintext
       tokens may still be in dead scripts triggering conflicts

  3. The 22MB rexxie_bot_stderr.log suggests the zombie plist may have
     been partially active at some point. Confirm com.hermes.rexxie-bot.plist
     stays DISABLED after fix.

  4. Consider adding a PID file check to rex_telegram_bot.py and
     rex_rexxie_telegram_bot.py to prevent duplicate launches.
